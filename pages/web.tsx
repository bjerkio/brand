import React from 'react';
import { webTheme } from '../theme/web/theme';
import Sheet from '.';

const WebTheme: React.FC = () => {
  return <Sheet theme={webTheme} name="Web" />;
};

export default WebTheme;
